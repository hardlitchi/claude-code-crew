# Port for the application
PORT=3002

# Working directory inside container
WORK_DIR=/app/workspace

# Host directory to mount as workspace
HOST_WORK_DIR=/home

# User ID for file permissions (current user)
USER_ID=1000

# Group ID for file permissions (current user)
GROUP_ID=1000

# Additional arguments for Claude Code sessions
CC_CLAUDE_ARGS=

# 利用可能なリポジトリのリスト（JSON配列形式）
CC_REPOSITORIES='[
    {"id":"crew","name":"claude-code-crew","path":"/app/workspace/claude/claude-code-crew","description":"Claude Code Crew"}
]'